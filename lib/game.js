(function () {
  if (typeof Asteroids === "undefined") {
    window.Asteroids = {};
  }

  function Game (dim_x, dim_y) {
    this.DIM_X = dim_x;
    this.DIM_Y = dim_y;
    this.NUM_ASTEROIDS;
  }


})();
